export const metadata = {
  title: "Опубликованные новости",
  description: "Управляем новостями компании",
};

export default function DashboardNewsLayout({ children }) {
  return <div>{children}</div>;
}
