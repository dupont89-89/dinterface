export const metadata = {
  title: "Редактировать новость",
  description: "Редактируем новости компании",
};

export default function EditNewsLayout({ children }) {
  return <div>{children}</div>;
}
