export const metadata = {
  title: "Управление приложением",
  description: "Управляем приложением. Все настройки здесь.",
};

export default function DashboardPageLayout({ children }) {
  return <div>{children}</div>;
}
