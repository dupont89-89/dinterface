export const metadata = {
  title: "Управление анкетами соискателями",
  description: "Добавляем комментарии и управляем пользователями нашей базы.",
};

export default function AnketItemLayout({ children }) {
  return <div>{children}</div>;
}
